[build]
  command = "npm install --legacy-peer-deps && npm run build:netlify"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  CI = "true"
  NODE_ENV = "production"

# 使用 Netlify 的 _redirects 文件来处理 SPA 路由